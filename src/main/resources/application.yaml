spring:
  application:
    name: financia-api
  datasource:
    url: jdbc:postgresql://localhost:5432/financia
    username: postgres
    password: 21062004
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
